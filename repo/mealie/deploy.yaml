apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: http-test
  name: http-test
spec:
  replicas: 10
  selector:
    matchLabels:
      app: http-test
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: http-test
    spec:
      containers:
        - image: httpd:alpine3.18
          name: httpd
  strategy: 
    type: RollingUpdate
    rollingUpdate:
    
